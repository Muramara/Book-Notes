<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Book Notes</title>
</head>
<body>
    <form action="/user" method="post">
        <% users.forEach(user => { %>
            <input type="submit" name="user" value="<%= user.name %>">
        <% }); %>
        <input type="submit" name="add" value="Add User">
    </form>
</body>
</html>